winged:
  config:
    enabled: true
    provider: properties
    data:
      default:
        pay:
          channels:
            - wx_pub
            - alipay_wap
          channelProperties:
            wx_pub:
              driver: BEECLOUD
              name: wx
